# Домашнее задание № 1

## Работа с изображениями, цыетовые пространства, фильтрация и морфология

Добавление шумов — одна из классических задач компьютерного зрения. В данной лабораторной работе разберем физические шумы и математические модели их генерации.

Наиболее распространённая модель шумов — аддитивный белый гауссов шум (AWGN), который представляет собой добавление к каждому пикселю нормально распределённого случайного значения:

\[
I_{\text{noisy}}(x, y) = I(x, y) + \mathcal{N}(\mu, \sigma^2)
\]

Импульсный шум представляет собой случайное замещение пикселей на минимальные (*перец*) или максимальные (*соль*) значения.

Каждый из типов шумов может быть удалён различными методами фильтрации. Разберем два основных подхода к фильтрации: размытие и морфологические операции.

Размытие — один из базовых алгоритмов обработки изображения. Размытие строится из предположения, что каждый пиксель картинки по своему значению связан с предыдущим и не должен заметно от него отличаться. Если же так происходит, например, вокруг пикселя яркость 240, а в самом пикселе 12, то мы предполагаем, что данный пиксель — это шумы. И размытие картинки — один из главных способов нивелировать эти шумы. Базовый подход к фильтрации строится на том, что значение пикселя должно измениться под влиянием окружающих пикселей.

В данной лабораторной работе будет подробно рассмотрен фильтр Гаусса как базовая часть алгоритма выявления контуров объектов на изображении.

Для начала переведем изображение в черно-белый формат. В этом случае каждый пиксель представляет собой только одно целое неотрицательное значение длиной 8 бит — яркость. Далее рассмотрим изображение как двумерную матрицу размерности \(h \times w\):

\[
A = \begin{pmatrix}
a_{11} & \cdots & a_{1w} \\
\vdots & \ddots & \vdots \\
a_{h1} & \cdots & a_{hw}
\end{pmatrix}
\]

Теперь рассмотрим произвольный пиксель данной матрицы \(a_{ij}\):

\[
\begin{matrix}
a_{i-1,j-1} & a_{i-1,j} & a_{i-1,j+1} \\
a_{i,j-1} & a_{i,j} & a_{i,j+1} \\
a_{i+1,j-1} & a_{i+1,j} & a_{i+1,j+1}
\end{matrix}
\]

Значение яркости данного пикселя будем менять с учетом всех указанных пикселей вокруг заданного. Для этого необходимо выполнить операцию свертки. Операция свертки заключается в преобразовании исходной матрицы \(B\) размерности \(n \times n\) в числовое значение с помощью специальной матрицы \(\text{ker}\) размерности \(n \times n\), называемой ядром свертки:

\[
val = \sum_{k=1}^{n} \sum_{l=1}^{n} B[k,l] \cdot \text{ker}[k,l] \tag{1}
\]

Матрица \(\text{ker}\) берется с различными значениями для разных типов фильтрации. Стандартное условие — данная матрица должна иметь сумму элементов, близкую к единице.

Гауссовский фильтр построен на ядре свертки, полученной как значения функции Гаусса (плотность распределения) для двумерной случайной величины:

\[
gauss[x,y] = \frac{1}{2\pi\sigma^2} e^{-\frac{(x-a)^2 + (y-b)^2}{2\sigma^2}}
\]

Здесь \(a, b\) — математическое ожидание двумерной случайной величины. Для построения матрицы в качестве этих значений выбираем координаты центрального элемента матрицы, например, для матрицы \(5 \times 5\) значения математического ожидания будут следующими: \(a = 3, b = 3\). В качестве \(x, y\) выбираем все индексы всех элементов матрицы поочередно. Значение \(\sigma\) будем считать параметром размытия по Гауссу. При каждом новом значении \(\sigma\) результат размытия будет отличаться.

Размерность матрицы свертки — это другой возможный параметр фильтрации.

Далее применяем операцию свертки ко всем внутренним пикселям изображения и записываем вычисленное по формуле (1) число в качестве нового значения яркости пикселя.

Разберем, что означает словосочетание «внутренний пиксель».

Пусть у нас изображение размерности \(480 \times 600\) лежит в матрице, нумерация индексов матрицы начинается с нуля. Допустим, мы применяем свертку размерности 21, тогда мы не можем обработать первые и последние 10 пикселей по горизонтали и по вертикали. Значит, мы будем обрабатывать пиксели с 10 по 469 и с 10 по 589.

Итого алгоритм кратко будет выглядеть следующим образом:

- установить размер матрицы свертки и среднее квадратичное отклонение;
- заполнить матрицу свертки значениями функции Гаусса с математическим ожиданием, равным координатам центра матрицы;
- нормировать матрицу так, чтобы сумма элементов равнялась 1;
- создать копию изображения;
- для каждого внутреннего пикселя копии рассчитать новое значение насыщенности пикселя по формуле (1) и **ЗАПИСАТЬ** это значение в пиксель нового изображения.

Работа с пикселями изображения осуществляется в OpenCV как с элементами двумерных матриц.

Приведение изображения в черно-белый формат было разобрано в предыдущей лабораторной работе.

Морфологическая фильтрация шума с помощью операций открытия (opening) и закрытия (closing) — это мощный и интуитивно понятный метод, основанный на форме объектов, а не на их спектральных характеристиках. Мы используем простую фигуру — структурный элемент (Structuring Element, SE) — и «проходим» ею по всему изображению. В зависимости от операции, мы либо удаляем объекты, в которые этот элемент не может полностью поместиться (открытие), либо заполняем отверстия, в которые элемент может поместиться (закрытие). Открытие и закрытие состоят из двух простых операций: эрозия и дилатация.

Вам необходимо разобрать математическую формулу этих операций самостоятельно.

Эрозия удаляет мелкие белые объекты (шум), разрывает тонкие перемычки, уменьшает объекты. Дилатация заполняет маленькие отверстия и разрывы, увеличивает объекты. Открытие, то есть эрозия и дилатация, удаляет все мелкие белые объекты (шум), сужает все крупные объекты, затем восстанавливает примерный размер крупных объектов (которые не были уничтожены эрозией), но не возвращает удаленный шум, так как он уже «стёрт» на этапе эрозии. Закрытие, то есть дилатация и эрозия, сначала заполняет маленькие черные отверстия (шум) и расширяет все объекты, затем восстанавливает примерный размер объектов, но не открывает снова заполненные отверстия, так как они стали частью объекта на этапе дилатации.

---


### Задание 1
Напишите метод toHSV(image_path), принимающий путь к изображению, и возвращающий фрейм openCV в цветовом пространстве HSV.
Протестируйте дополнительно вывод изображений на экран в RGB и HSV.

### Задание 2
Напишите метод, grascale_video(video_input_path, video_output_path), которое читает видео, переводит его в градации серого и сохраняет по введенному адресу. Параметры видео должны сохранится неизменными.

### Задание 3
Постройте свою реализацию метода построения гистограммы черно-белого изображения toHistogram(image), при условии, что аргументом передаём изображение в BGR формате

### Задание 4
Постройте свою реализацию расчёта порога бинаризации методом ОЦУ get_OTSU_thresh(image) для изображения, передаваемого в формате BGR

### Задание 5
Напишите метод get_all_binaries(image), который принимает изображение в формате BGR и возвращает множество всех бинарных изображений, полученных применением всех встроенных режимов threshold

### Задание 6
Напишите метод add_gauss_noise(image, sigma), который принимает изображение в BGR формате и возвращает ЧБ изображение с добавленным шумом

### Задание 7
Напишите метод add_saltpepper_noise(image, p), который принимает изображение в BGR формате и возвращает ЧБ изображение с добавленным импульсным шумом

### Задание 8 `compute_centered_spectrum(image: np.ndarray) -> np.ndarray`

**Описание:**  
Выполняет двумерное БПФ и сдвигает нулевую частоту в центр.

**Вход:**  
- `image` – двумерный массив `np.uint8` (H×W).

**Выход:**  
- Комплексный массив `np.complex128` размера (H×W) – центрированный спектр.

---

### Задание 9 `compute_magnitude_spectrum(spectrum: np.ndarray) -> np.ndarray`

**Описание:**  
Вычисляет логарифмический амплитудный спектр (`log(|спектр| + 1)`).

**Вход:**  
- `spectrum` – центрированный комплексный спектр.

**Выход:**  
- Двумерный массив `np.float64` той же размерности.

---

### Задание 10 `create_noise_mask(shape: tuple, peaks: list) -> np.ndarray`

**Описание:**  
Создаёт комплексную маску размера `shape`, заполненную нулями, и добавляет в заданные координаты пики с учётом симметрии.

**Вход:**  
- `shape` – кортеж `(height, width)`.  
- `peaks` – список кортежей `(u, v, amplitude)`, где `u`, `v` – смещения от центра (целые), `amplitude` – действительная амплитуда.

**Выход:**  
- Комплексный массив `np.complex128` размера `shape`, где в точках `(center+u, center+v)` и `(center-u, center-v)` добавлены значения `amplitude` и `np.conj(amplitude)` соответственно. Для центральной точки добавляется только одно значение.

---

### Задание 11 `add_noise_to_spectrum(spectrum: np.ndarray, mask: np.ndarray) -> np.ndarray`

**Описание:**  
Поэлементно складывает спектр и маску шума.

**Вход:**  
- `spectrum` – центрированный комплексный спектр.  
- `mask` – комплексная маска того же размера.

**Выход:**  
- Новый комплексный массив – сумма `spectrum + mask`.

---

### Задание 12 `restore_image_from_spectrum(spectrum: np.ndarray) -> np.ndarray`

**Описание:**  
Выполняет обратное преобразование: снимает центрирование, `ifft2`, берёт действительную часть, клиппирует и приводит к `uint8`.

**Вход:**  
- `spectrum` – центрированный комплексный спектр.

**Выход:**  
- Изображение `np.uint8` той же размерности.

---

### Задание 13 `add_frequency_noise(image: np.ndarray, peaks: list) -> np.ndarray`

**Описание:**  
Объединяет шаги 1–5: получает спектр, создаёт маску, добавляет шум, восстанавливает изображение. Главная функция для тестов.

**Вход:**  
- `image` – исходное изображение `np.uint8`.  
- `peaks` – список пиков (см. п.3).

**Выход:**  
- Зашумлённое изображение `np.uint8`.


### Задание 14
Напишите метод getGaussian(n, sigma), возвращающую матрицу размытия по Гауссу

### Задание 15
Реализуйте фильтр Гаусса myGaussBlur(image, n, sigma), принимающую изображения в градациях серого.

### Задание 16
Постройте реализацию двух методов морфологических операций open_close(image) и close_open(image), принимающих изображение в градациях серого и применяющих бинаризацию ОЦУ.

### Задание 17 `create_ideal_lowpass_filter(shape: tuple, cutoff: int) -> np.ndarray`

**Описание:**  
Создаёт идеальный фильтр нижних частот в частотной области (центрированный). Значения 1 внутри круга радиуса `cutoff` и 0 вне.

**Вход:**  
- `shape` – кортеж `(height, width)`.  
- `cutoff` – целое число, радиус в пикселях.

**Выход:**  
- Двумерный массив `np.float64` размера `shape`, значения 0 или 1.

---

### Задание 18 `create_gaussian_lowpass_filter(shape: tuple, cutoff: float) -> np.ndarray`

**Описание:**  
Создаёт гауссовский фильтр нижних частот: `exp(-(u^2+v^2)/(2*cutoff^2))` (центрированный).

**Вход:**  
- `shape` – кортеж `(height, width)`.  
- `cutoff` – стандартное отклонение (сигма) в пикселях.

**Выход:**  
- Двумерный массив `np.float64` размера `shape` со значениями в диапазоне (0,1].

---

### Задание 19 `create_ideal_highpass_filter(shape: tuple, cutoff: int) -> np.ndarray`

**Описание:**  
Создаёт идеальный фильтр верхних частот (1 – низкочастотный фильтр). Значения 0 внутри круга радиуса `cutoff` и 1 вне.

**Вход:**  
- `shape` – кортеж `(height, width)`.  
- `cutoff` – целое число, радиус в пикселях.

**Выход:**  
- Двумерный массив `np.float64` размера `shape`, значения 0 или 1.

---

### Задание 20 `create_gaussian_highpass_filter(shape: tuple, cutoff: float) -> np.ndarray`

**Описание:**  
Создаёт гауссовский фильтр верхних частот: `1 - exp(-(u^2+v^2)/(2*cutoff^2))`.

**Вход:**  
- `shape` – кортеж `(height, width)`.  
- `cutoff` – стандартное отклонение в пикселях.

**Выход:**  
- Двумерный массив `np.float64` размера `shape` со значениями в диапазоне [0,1).

### Задание 21 `create_bandpass_filter(shape: tuple, low_cutoff: int, high_cutoff: int) -> np.ndarray`

**Описание:**  
Создаёт полосовой фильтр (идеальный кольцевой): 1 для частот между `low_cutoff` и `high_cutoff`, 0 иначе.

**Вход:**  
- `shape` – кортеж `(height, width)`.  
- `low_cutoff` – внутренний радиус.  
- `high_cutoff` – внешний радиус.

**Выход:**  
- Двумерный массив `np.float64` размера `shape` со значениями 0 или 1.

### Задание 22 `apply_filter_to_spectrum(spectrum: np.ndarray, filter_mask: np.ndarray) -> np.ndarray`

**Описание:**  
Применяет фильтр к центрированному комплексному спектру поэлементным умножением.

**Вход:**  
- `spectrum` – центрированный комплексный спектр (`np.complex128`).  
- `filter_mask` – вещественный массив того же размера (значения от 0 до 1).

**Выход:**  
- Новый комплексный массив – результат умножения `spectrum * filter_mask`.

### Задание 22 `filter_image(image: np.ndarray, filter_mask: np.ndarray) -> np.ndarray`

**Описание:**  
Выполняет полный цикл фильтрации: получает центрированный спектр, умножает на маску фильтра, восстанавливает изображение (с помощью `restore_image_from_spectrum` из предыдущего задания, или реализовать аналогично). Возвращает отфильтрованное изображение.

**Вход:**  
- `image` – исходное изображение `np.uint8`.  
- `filter_mask` – фильтр в частотной области (центрированный, вещественный).

**Выход:**  
- Отфильтрованное изображение `np.uint8`.


### Задание 23
Подготовьте несколько зашумлений различных типов одного изображения: 2 изображения с гауссовым шумом, 2 изображения с импульсным шумом, 2 изображения с комбинированным зашумлением. Примените к изображениям разные типы встроенных фильтраций:
- гауссов фильтр с тремя комбинациями параметров;
- медианный фильтр с двумя комбинациями параметров;
- две комбинации морфологических операций (открытие-закрытие и наоборот).

Сделайте выводы и запишите в readme-файл все параметры эксперимента и его результаты.

---

## Контрольные вопросы

1. Опишите, в чем заключается принцип операции размытия изображения и зачем ее применяют?
2. Что такое операция свертки матрицы?
3. Каким образом строится матрица свертки в размытии по Гауссу? Какие могут быть в ней значения? Зачем проводится нормализация?
4. Опишите алгоритм размытия по Гауссу.
5. Опишите, что такое линейные фильтры, нелинейные фильтры, какие бывают?
6. Опишите, что такое частотные шумы, как они возникают, как их можно замоделировать?
7. Что такое дискретное преобразование Фурье?
8. Какие методы зашумления изображения Вы знаете?
9. Расскажите, какие методы бинаризации изображений в OpenCV вы знаете?
10. Расскажите, какие морфологические операции над бинаризированными изображениями Вы знаете? Опишите, что они позволяют сделать на изображении.
11. Опишите, как происходит частотная фильтрация?
12. Опишите, какие методы фильтрации лучше применять для каких типов шумов.

---
